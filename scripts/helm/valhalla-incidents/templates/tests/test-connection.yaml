apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "valhalla-with-incidents.fullname" . }}-test-connection"
  labels:
    {{- include "valhalla-with-incidents.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "valhalla-with-incidents.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
