apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "valhalla-incidents.fullname" . }}-test-connection"
  labels:
    {{- include "valhalla-incidents.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "valhalla-incidents.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
