apiVersion: v1
kind: Service
metadata:
  name: {{ include "valhalla-with-incidents.fullname" . }}
  labels:
    {{- include "valhalla-with-incidents.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  clusterIP: None
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "valhalla-with-incidents.selectorLabels" . | nindent 4 }}
